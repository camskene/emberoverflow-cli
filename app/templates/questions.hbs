<div class="row">
  <div class="col-md-6">
    <h2>{{siteTitle}}</h2>
    <p>It is {{current-date 'dddd'}}.</p>
    <ul>
      {{#each}}
        {{question-preview question=this}}
      {{/each}}
    </ul>
  </div>
  <div class="col-md-6">
    {{outlet}}
  </div>
</div>


